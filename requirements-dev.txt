-r requirements.txt

# Dev / testing
pytest>=7.4
pytest-cov>=4.1
ruff>=0.1
black>=23.12

# Notebooks
jupyter>=1.0
ipykernel>=6.27
matplotlib>=3.8
contextily>=1.5

# PDF to image conversion (one-time prep for Master Plan maps)
pymupdf>=1.24

# OCR for Master Plan zone-label extraction (pipeline only; kept OUT of
# requirements.txt so the app deploy doesn't pull torch ~1 GB).
easyocr>=1.7,<2.0